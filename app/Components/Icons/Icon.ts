import { CheckIcon } from "./icons/CheckIcon";
import { LogoIcon } from "./icons/LogoIcon";

export interface IconProps {
  className?: string;
}

export const Icon = {
  Logo: LogoIcon,
  Check: CheckIcon,
};
